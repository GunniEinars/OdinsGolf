<resources>
    <!-- Normal app theme: solid black window behind the dark Compose UI. -->
    <style name="Theme.OdinsGolf" parent="@android:style/Theme.DeviceDefault">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:colorBackground">@color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Launch theme using the Android SplashScreen API: the OS shows our logo,
         centered at the standard splash size on white, then opens straight into
         the app (no separate in-app splash page). -->
    <style name="Theme.OdinsGolf.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/odins_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.OdinsGolf</item>
    </style>
</resources>
